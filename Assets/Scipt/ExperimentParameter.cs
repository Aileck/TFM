using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ExperimentParameter : MonoBehaviour
{
    // Start is called before the first frame update
    public string id;
    public string group;
    public bool avatar;
    public bool training;

    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
